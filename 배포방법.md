# 통합 시식 설문 — 배포 방법 (처음부터 순서대로)

폰으로 QR 찍고 응답 → **Supabase 한 곳에 누적** → 대시보드가 실시간 집계.
claude.ai 계정 없이 **어느 팀이든 폰 브라우저로** 참여 가능.

폴더 구성:
- `index.html`   ← 응답 폼 (이 주소를 QR로 공유)
- `results.html` ← 결과 대시보드 (사장·본부만 공유)
- `questions.json` ← **시식 내용 바꿀 때 이 파일만 수정** (제품·문항·가격 등)
- `config.js`    ← Supabase 접속 키 (아래 2단계에서 입력)
- `qr.html`      ← QR 생성기
- `supabase-setup.sql` ← DB 테이블 생성 SQL

---

## 1단계 · Supabase 테이블 만들기 (1분)
1. https://supabase.com 로그인 → 프로젝트 선택
   (FoodCost 프로젝트 재사용 가능 — 테이블 이름이 `svy_` 로 분리돼 안 섞임)
2. 왼쪽 메뉴 **SQL Editor → New query**
3. `supabase-setup.sql` 내용 전체 복사 → 붙여넣고 **RUN**
   → `svy_surveys`, `svy_responses` 두 테이블 생성됨

## 2단계 · 접속 키 입력 (1분)
1. 같은 프로젝트에서 **⚙ Settings → API**
2. 두 값 복사:
   - **Project URL**
   - **Project API keys → anon / public**
3. `config.js` 를 열어 두 자리에 붙여넣기 (`여기에_..._붙여넣기` 를 교체)

## 3단계 · 공개 배포 (Cloudflare Pages · 2분)
1. https://dash.cloudflare.com → **Workers & Pages → Create → Pages**
2. **Upload assets** (직접 업로드) 선택
3. `D:\설문\통합시식설문` 폴더 통째로 드래그&드롭
   ※ `node_modules`, `serve.js`, `.claude` 는 없어도 됨 (있어도 무방)
4. 배포되면 `https://<프로젝트>.pages.dev` 주소가 나옴
   - 응답 폼: `.../index.html`
   - 대시보드: `.../results.html`

## 4단계 · QR 만들어 공유
1. `qr.html` 를 브라우저로 열기
2. 위 `.../index.html` 주소 붙여넣고 **QR 만들기** → 인쇄·게시
3. 팀원들이 폰으로 스캔 → 응답 → `results.html` 에 실시간 누적

---

## 다음 시식 때 (내용만 바꾸기)
- `questions.json` 의 `slug`(회차명)·제품·문항만 수정하고 재배포.
- **고정 5축(맛·향·모양새·식감·제공량)은 그대로 두세요.** 그래야 회차가 쌓여도 비교가 됩니다.
- 대시보드 우측 상단 회차 선택으로 지난 회차도 조회 가능.

## 로컬 테스트 (선택)
```
cd D:\설문\통합시식설문
node serve.js   →  http://localhost:4500
```
